#include <iostream>


using namespace std;

int main()
{
    float a, b, c, d;
    cin >> a >> b >> c;
    if ((0 < a && a < 30000) && (0 < b && b < 30000) && (0 < c && c < 30000)) {
        d = sqrt((a * a) + (b * b) + (c * c));
    }
    cout << d;
    return 0;
}
